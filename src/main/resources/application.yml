spring:
  main:
    allow-circular-references: true
  application:
    name: ${APPLICATION_NAME:media-backend-server}
  data:
    redis:
      port: ${REDIS_PORT:6379}
      host: ${REDIS_HOST:115.73.218.193}
      database: 0
  datasource:
      url: ${DB_URL:jdbc:postgresql://localhost:5432/roomify_media_db}
      username: ${DB_USERNAME:admin}
      password: ${DB_PASSWORD:huytuanphuc@!}
      driver-class-name: ${DB_DRIVER:org.postgresql.Driver}

  sql:
    init:
      mode: always
  jpa:
    database-platform: ${DB_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:862237343446-g75pjvd3pvnfq6i0pvlblfj1d5o2a4t1.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET: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}
            redirect-uri: ${GOOGLE_REDIRECT_URL:http://localhost:8081/login/oauth2/code/google}
            scope:
              - email
              - profile
            client-name: Google
          github:
            client-id: ${GITHUB_CLIENT_ID:Iv23lij89v38aA4qUVfg}
            client-secret: ${GITHUB_CLIENT_SECRET:6b9b87b1da688e120d720629bc26720bc68ba7b1}
            redirect-uri: ${GITHUB_REDIRECT_URL:http://localhost:8081/login/oauth2/code/github}
            scope:
              - read:user
              - user:email
            client-name: GitHub
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user
server:
  port: ${SERVER_PORT:8081}

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

logging:
  level:
    org.springframework.web.socket=DEBUG:
    org.springframework.web=DEBUG:

jwt:
  url: /api/v1/login/non-type
  header: Authorization
  prefix: Bearer
  expiration: 360000
  secret: ${JWT_SECRET:FBu8YFbkcrgG9UUFbBKTTlkO5Md8Nd5Cf7O2V4uqs1I=}
